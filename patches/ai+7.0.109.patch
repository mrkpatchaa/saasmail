diff --git a/node_modules/ai/dist/index.js b/node_modules/ai/dist/index.js
--- a/node_modules/ai/dist/index.js
+++ b/node_modules/ai/dist/index.js
@@ -1,6 +1,9 @@
     body,
     messageId
   }) {
     const abortController = new AbortController();
+    const seedToolContinuation =
+      trigger === "resume-stream" &&
+      this.transport?._expectToolContinuation === true;
     const activeResumeRequest = trigger === "resume-stream" ? { abortController } : void 0;
@@ -1,8 +1,10 @@
       const response = {
         state: createStreamingUIMessageState({
           lastMessage:
-            trigger === "resume-stream" || trigger === "regenerate-message"
+            trigger === "regenerate-message" ||
+            (trigger === "resume-stream" && !seedToolContinuation)
               ? void 0
               : this.state.snapshot(lastMessage),
           messageId: this.generateId()
